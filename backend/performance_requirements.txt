# Performance Testing Dependencies for PDFTablePro
# Install with: pip install -r performance_requirements.txt

# Core async HTTP client for load testing
aiohttp>=3.9.0

# System monitoring and resource tracking
psutil>=5.9.0

# Advanced statistics for performance metrics
numpy>=1.24.0
scipy>=1.10.0

# Optional: Redis for distributed rate limiting (production)
redis>=5.0.0

# Optional: Celery for background task processing (production)
celery>=5.3.0

# Optional: Performance profiling tools
line_profiler>=4.1.0
memory_profiler>=0.61.0

# Optional: Load testing with locust (alternative approach)
locust>=2.17.0

# Optional: Monitoring and metrics
prometheus-client>=0.19.0