# PDF Table Extractor - Railway-Compatible Dependencies
# Simplified version for deployment (problematic dependencies removed)

# FastAPI and server components
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
slowapi==0.1.9
python-dotenv==1.0.0
supabase==2.3.0
python-jose[cryptography]==3.3.0

# PDF processing libraries (Railway-compatible)
pypdf==3.17.4
pdfplumber==0.10.3
# NOTE: camelot-py and tabula-py removed - cause deployment failures

# Data processing and export
pandas==2.1.4
openpyxl==3.1.2

# Security and validation
pydantic==2.5.2
# NOTE: python-magic-bin replaced with python-magic for Linux compatibility
python-magic==0.4.27

# Development and testing (optional for deployment)
pytest==7.4.3
pytest-asyncio==0.21.1