[phases.setup]
nixPkgs = ['python310', 'pip', 'poppler-utils']

[phases.install]
cmds = [
  'python -m pip install --upgrade pip',
  'pip install -r requirements.txt'
]

[phases.build]
cmds = []

[start]
cmd = 'python start.py'